# CLAUDE.md — Правила для AI-агентов

## 🎯 О ПРОЕКТЕ

**AI CITI | НЕЙРОГОРОД** — геймифицированная платформа для сетевиков.
Telegram Mini App + Web + Desktop (один код).

**Текущий модуль:** НЕЙРОПОСТЕР — планировщик публикаций Instagram с автопубликацией.

**Статус:** MVP готов ✅ | Работаем над улучшениями

---

## 👑 АДМИН-ПАНЕЛЬ (RULES)

### Telegram ID Админов
```typescript
const ADMIN_IDS = [643763835, 190202791]
```

### Структура Админ-панели
```
/admin
├── CRM                    # Управление клиентами
│   ├── Все пользователи   # Таблица всех юзеров
│   ├── Платные клиенты    # Премиум подписки
│   ├── Рассылка           # Массовые сообщения
│   └── Аналитика          # Дашборд KPI
│
├── Школа (Курс)           # Образовательный модуль
│   ├── Модули             # Структура курса
│   ├── Уроки              # Контент уроков
│   ├── Ученики            # Прогресс учеников
│   └── Проверка ДЗ        # Домашние задания
│
└── Квизы                  # Система тестирования
    ├── Список квизов
    ├── Создание/Редактирование
    └── Аналитика ответов
```

### Правила доступа
- Пункт "Админ" в меню приложения виден ТОЛЬКО для `ADMIN_IDS`
- Проверка через `telegramUser.id` в TMA или localStorage в браузере
- При попытке доступа без прав → редирект на главную

### Визуальный стиль админки
- Тёмная тема: `bg-zinc-900`, `border-zinc-800`
- Акцентные цвета: жёлтый/золотой для VIP элементов
- Карточки с иконками и счётчиками
- Premium UI: glassmorphism, subtle animations

---

## 🔑 ДОСТУПЫ И CREDENTIALS

**ВСЕ КЛЮЧИ ХРАНЯТСЯ В `.env.local`** — читай этот файл в начале сессии!

```bash
cat /Users/bekk/Desktop/AI-CITI/.env.local
```

**Содержит:**
- Supabase: URL, Anon Key, Service Role Key, Access Token
- Cloudinary: Cloud Name, API Key, API Secret
- N8N: API URL, API Key, Workflow ID

**Как использовать:**
- Supabase REST API: используй Service Role Key для обхода RLS
- Supabase SQL: используй Access Token с Management API
- Cloudinary: для загрузки/удаления фото
- N8N: для управления workflows

**Пример SQL запроса через Supabase Management API:**
```bash
curl -X POST "https://api.supabase.com/v1/projects/debcwvxlvozjlqkhnauy/database/query" \
  -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"query": "SELECT * FROM user_photo_gallery"}'
```

---

## ⚠️ КРИТИЧЕСКИЕ ПРАВИЛА ДЛЯ АГЕНТОВ

### 🚨 ПРАВИЛО #1: ВСЕГДА ПУШИТЬ В GIT (БЕЗ ИСКЛЮЧЕНИЙ!)

**ОБЯЗАТЕЛЬНО после КАЖДОГО изменения:**

```bash
git add . && git commit -m "feat: описание" && git push
```

**Почему это критично:**
- Заказчики работают в двоем с одним аккаунтом Cursor на разных устройствах
- Они НЕ знают код, НЕ знают git, НЕ знают терминал
- Они работают ТОЛЬКО через чат с тобой
- Если ты не запушишь — изменения потеряются при переключении устройств
- Другой человек не увидит твои изменения
- Синхронизация между устройствами сломается

**НЕ ДЕЛАЙ НИКОГДА:**
- ❌ "Сделаю коммит потом" — НЕТ! Сразу после изменений!
- ❌ "Соберу несколько изменений в один коммит" — НЕТ! Каждое изменение отдельно!
- ❌ "Попрошу заказчика закоммитить" — НЕТ! Он не знает как!
- ❌ "Забыл запушить" — НЕТ! Это критическая ошибка!

**ДЕЛАЙ ВСЕГДА:**
- ✅ После каждого изменения файла — сразу `git add . && git commit -m "..." && git push`
- ✅ Если изменил несколько файлов — один коммит, но ВСЕГДА с push
- ✅ Если не уверен — лучше закоммитить дважды, чем потерять изменения
- ✅ После каждого ответа в чате, где ты что-то изменил — проверь, что запушил

---

### 🚨 ПРАВИЛО #2: РАБОТА ТОЛЬКО ЧЕРЕЗ ЧАТ (ЗАКАЗЧИКИ НЕ ЗНАЮТ КОД)

**Контекст:**
- Заказчики НЕ программисты
- Они НЕ знают TypeScript, React, SQL, git, терминал
- Они НЕ работают с кодом напрямую
- Они работают ТОЛЬКО через чат с тобой

**Что это значит:**
- Ты делаешь ВСЁ сам: код, проверки, коммиты, push
- Заказчик только говорит "сделай то-то" или "исправь это"
- Ты НЕ просишь заказчика запустить команды
- Ты НЕ объясняешь технические детали (если не спросят)
- Ты НЕ просишь заказчика что-то проверить в коде

**Примеры:**

❌ **НЕПРАВИЛЬНО:**
```
"Теперь запусти `npm run build` чтобы проверить"
"Сделай git commit и push"
"Открой файл и измени строку 42"
"Проверь, работает ли это"
```

✅ **ПРАВИЛЬНО:**
```
[Делаю изменения в коде]
[Запускаю npm run build сам]
[Если ошибки — исправляю сам]
[Делаю git add . && git commit -m "..." && git push]
"✅ Готово! Добавил функцию X. Изменения запушены в GitHub."
```

---

### 🚨 ПРАВИЛО #3: ПРОВЕРКА ПЕРЕД ПУШЕМ (ДЕЛАЙ САМ!)

**Перед `git push` ОБЯЗАТЕЛЬНО (делай сам, не проси заказчика):**

1. ✅ Проверь, что код компилируется:
   ```bash
   npm run build
   ```
   Если ошибки — исправь ДО коммита! Не говори заказчику про ошибки, просто исправь.

2. ✅ Проверь TypeScript ошибки:
   ```bash
   npx tsc --noEmit
   ```
   Если ошибки — исправь ДО коммита! Не говори заказчику про ошибки, просто исправь.

3. ✅ Убедись, что изменил правильные файлы:
   ```bash
   git status
   ```
   Если видишь файлы других агентов — НЕ коммить! Спроси заказчика разрешения.

4. ✅ Напиши понятное сообщение коммита:
   ```bash
   git commit -m "feat: добавил функцию X"
   # или
   git commit -m "fix: исправил баг Y"
   ```

5. ✅ ТОЛЬКО ПОСЛЕ ВСЕГО ЭТОГО — push:
   ```bash
   git push
   ```

**Важно:** Все проверки делай САМ через терминал. Не проси заказчика ничего проверять!

---

## 🛠️ ТЕХНОЛОГИЧЕСКИЙ СТЕК (СТРОГО!)

```
Frontend:   React 18 + Vite + TypeScript
Стили:      TailwindCSS + shadcn/ui
Роутинг:    React Router 6
State:      Zustand + React Query
Backend:    Supabase Edge Functions (Deno) + n8n webhooks
База:       Supabase PostgreSQL + RLS
Storage:    Supabase Storage
Деплой:     Vercel
```

### ⛔ НЕ ИСПОЛЬЗОВАТЬ:
- Next.js
- Express / Node.js backend (только Edge Functions)
- Другие UI библиотеки (только shadcn/ui)
- CSS файлы (только Tailwind классы)
- Другие state менеджеры (только Zustand)

---

## 👥 КОМАНДА АГЕНТОВ

| Агент | Зона | Файлы |
|-------|------|-------|
| **АНЯ** | База данных | `supabase/migrations/*.sql` |
| **БОРЯ** | Backend | `supabase/functions/*`, n8n webhooks |
| **ВАСЯ** | Frontend | `src/**/*.tsx`, `src/**/*.ts`, `src/**/*.css` |

### ⚠️ КРИТИЧЕСКОЕ ПРАВИЛО:
**Один файл = один агент. Агенты НЕ пересекаются!**

**Если нужно изменить файл другого агента:**
1. Спроси у пользователя разрешения
2. Объясни, почему это нужно
3. Только после одобрения — меняй

---

## 📁 СТРУКТУРА ПРОЕКТА

```
ai-citi/
├── src/
│   ├── pages/              # Страницы (роуты)
│   │   ├── Home.tsx
│   │   ├── Profile.tsx
│   │   ├── Agents.tsx
│   │   ├── School.tsx
│   │   └── tools/
│   │       └── poster/     # НЕЙРОПОСТЕР
│   │           ├── index.tsx      ✅ Дашборд
│   │           ├── create.tsx    ✅ Создание
│   │           ├── edit.tsx      ✅ Редактирование
│   │           └── calendar.tsx  ✅ Календарь
│   ├── components/         # React компоненты
│   │   ├── ui/             # shadcn/ui
│   │   ├── layout/         # Header, Footer, Navigation
│   │   └── carousel/       # Компоненты карусели
│   ├── hooks/              # React хуки
│   │   ├── useAuth.ts
│   │   ├── usePosts.ts     # Хуки для постов
│   │   └── useCourse.ts
│   ├── store/              # Zustand stores
│   │   ├── authStore.ts
│   │   ├── carouselStore.ts
│   │   └── uiStore.ts
│   ├── lib/                # Утилиты
│   │   ├── supabase.ts
│   │   ├── telegram.ts
│   │   └── utils.ts
│   └── types/              # TypeScript типы
│       └── index.ts
│
├── supabase/
│   ├── migrations/         # SQL миграции (АНЯ)
│   │   ├── 001_users.sql
│   │   ├── 002_poster.sql
│   │   └── ...
│   └── functions/          # Edge Functions (БОРЯ)
│       └── auth-telegram/
│
├── public/                 # Статика
├── docs/                   # Документация
│   ├── ROADMAP.md          # Дорожная карта
│   ├── PROJECT_SUMMARY.md  # Резюме проекта
│   ├── TEAM_STATUS.md     # Статус команды
│   └── SYNC_INSTRUCTIONS.md # Инструкции синхронизации
└── .cursor/                # Настройки Cursor
    └── rules/              # Правила для агентов
```

---

## 🎨 СТИЛЬ КОДА

### TypeScript

```typescript
// ✅ Правильно
interface User {
  id: string
  telegramId: number
  username?: string
}

// ❌ Неправильно
type User = {
  id: any
  telegram_id: number
}
```

### React компоненты

```typescript
// ✅ Правильно — функциональные компоненты + хуки
export function PostCard({ post }: { post: Post }) {
  const [isLoading, setIsLoading] = useState(false)
  
  return (
    <div className="rounded-lg bg-white/10 p-4">
      {post.caption}
    </div>
  )
}

// ❌ Неправильно — классовые компоненты
class PostCard extends React.Component {}
```

### Tailwind классы

```tsx
// ✅ Правильно
<button className="rounded-lg bg-primary px-4 py-2 text-white hover:bg-primary/90">

// ❌ Неправильно — inline styles
<button style={{ backgroundColor: 'orange' }}>
```

### Именование

- Компоненты: `PascalCase` — `PostCard.tsx`
- Функции: `camelCase` — `getUserPosts()`
- Константы: `UPPER_SNAKE_CASE` — `MAX_POSTS = 10`
- Файлы: совпадают с экспортом — `PostCard.tsx` экспортирует `PostCard`

---

## 🗄️ БАЗА ДАННЫХ (Supabase)

### Именование таблиц
- `snake_case` множественное число: `scheduled_posts`, `post_media`
- Всегда `id UUID PRIMARY KEY DEFAULT gen_random_uuid()`
- Всегда `created_at TIMESTAMPTZ DEFAULT NOW()`
- Всегда `updated_at TIMESTAMPTZ DEFAULT NOW()`

### RLS политики (ОБЯЗАТЕЛЬНО!)

```sql
ALTER TABLE scheduled_posts ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users can manage own posts" ON scheduled_posts
  FOR ALL USING (user_id = auth.uid());
```

**Правило:** Каждая таблица ДОЛЖНА иметь RLS политики!

### Миграции (АНЯ)

- Нумерация: `001_description.sql`, `002_description.sql`
- Всегда добавляй комментарии в начале файла
- Всегда проверяй синтаксис перед коммитом

---

## 🔐 ПЕРЕМЕННЫЕ ОКРУЖЕНИЯ

### В .env.local (фронт)
```env
VITE_SUPABASE_URL=https://xxx.supabase.co
VITE_SUPABASE_ANON_KEY=xxx
VITE_TELEGRAM_BOT_USERNAME=xxx
VITE_META_APP_ID=xxx
VITE_N8N_PUBLISH_WEBHOOK=xxx
```

### В Supabase Secrets (секретные)
```bash
supabase secrets set TELEGRAM_BOT_TOKEN=xxx
supabase secrets set META_APP_SECRET=xxx
supabase secrets set INSTAGRAM_ACCESS_TOKEN=xxx
```

**⚠️ ВАЖНО:** Никогда не коммить `.env.local` в git!

---

## 🔧 N8N API (для агентов)

**N8N URL:** https://n8n.iferma.pro  
**API Key:** См. `/docs/N8N_API.md`

**Использование:**
- Агенты могут создавать workflow через N8N API
- Используй заголовок `X-N8N-API-KEY` во всех запросах
- Подробные инструкции: `/docs/N8N_API.md`

**Существующие webhooks:**
- `/webhook/neuroposter-publish` — публикация постов в Instagram
- `/webhook/client-carousel` — генерация каруселей

---

## ✅ ЧЕКЛИСТ ПЕРЕД КОММИТОМ (ОБЯЗАТЕЛЬНО!)

1. [ ] `npm run build` проходит без ошибок
2. [ ] `npx tsc --noEmit` не показывает ошибок
3. [ ] Изменил только свои файлы (проверил через `git status`)
4. [ ] Написал понятное сообщение коммита
5. [ ] **СДЕЛАЛ `git push`** (критично!)

**Если что-то не проходит — НЕ КОММИТЬ! Исправь сначала!**

---

## 🚀 КОМАНДЫ

### Разработка
```bash
# Запуск dev сервера
npm run dev

# Сборка
npm run build

# Проверка типов
npx tsc --noEmit

# Линтинг
npm run lint
```

### Git (ВСЕГДА после изменений!)
```bash
# Проверка статуса
git status

# Добавить все изменения
git add .

# Коммит с понятным сообщением
git commit -m "feat: описание"

# ПУШ (ОБЯЗАТЕЛЬНО!)
git push
```

### Supabase (АНЯ и БОРЯ)
```bash
# Применить миграции
supabase db push

# Деплой Edge Functions
supabase functions deploy [function-name]

# Посмотреть логи
supabase functions logs [function-name]
```

---

## 📚 ДОКУМЕНТАЦИЯ

- `/docs/ROADMAP.md` — дорожная карта (обновлена)
- `/docs/PROJECT_SUMMARY.md` — резюме проекта
- `/docs/TEAM_STATUS.md` — статус команды (обновляй после каждого коммита!)
- `/docs/NEUROPOSTER_TZ.md` — ТЗ на Нейропостер
- `/docs/SYNC_INSTRUCTIONS.md` — инструкции синхронизации
- `/docs/N8N_API.md` — инструкции по работе с N8N API (ключ, создание workflow)
- `/SUPABASE_TABLES_REPORT.md` — структура базы данных

---

## 🎯 WORKFLOW ДЛЯ АГЕНТОВ

### Когда получаешь задачу от заказчика:

1. **Прочитай задачу внимательно**
2. **Проверь, твоя ли это зона ответственности**
3. **Если нет — спроси у заказчика разрешения**
4. **Сделай `git pull` чтобы получить последние изменения (делай сам!)**
5. **Выполни задачу (делай сам!)**
6. **Проверь код сам (`npm run build`, `npx tsc --noEmit`)**
7. **ОБЯЗАТЕЛЬНО: `git add . && git commit -m "..." && git push` (делай сам!)**
8. **Сообщи заказчику, что готово (простыми словами, без технических деталей)**

### Пример диалога:

**Заказчик:** "Добавь кнопку удаления поста"

**Агент (ВАСЯ):**
```
[Читаю задачу]
[Проверяю: это фронтенд, моя зона]
[Делаю git pull сам]
[Добавляю кнопку в компонент]
[Проверяю npm run build сам - OK]
[Делаю git add . && git commit -m "feat: добавил кнопку удаления поста" && git push сам]
"✅ Готово! Кнопка удаления добавлена. Изменения запушены в GitHub."
```

**НЕ говори:** "Теперь проверь в браузере" или "Запусти npm run dev" — заказчик не знает как!

---

## 🚨 ЧАСТЫЕ ОШИБКИ (НЕ ДЕЛАЙ!)

1. ❌ **Забыл сделать git push** — КРИТИЧЕСКАЯ ОШИБКА!
   - ✅ Решение: ВСЕГДА делай push сразу после коммита. Проверяй после каждого ответа.

2. ❌ **Попросил заказчика запустить команду** — НЕПРАВИЛЬНО!
   - ✅ Решение: Делай всё сам через терминал. Заказчик не знает команды.

3. ❌ **Закоммитил с ошибками**
   - ✅ Решение: ВСЕГДА проверяй `npm run build` перед коммитом. Исправляй ошибки сам.

4. ❌ **Изменил файл другого агента без разрешения**
   - ✅ Решение: Спроси разрешения у заказчика перед изменением.

5. ❌ **Не понял задачу, но начал делать**
   - ✅ Решение: Уточни у заказчика, если не понятно. Лучше спросить, чем сделать не то.

6. ❌ **Объяснил заказчику технические детали без запроса**
   - ✅ Решение: Говори простыми словами. Технические детали только если спросят.

---

## 💡 СОВЕТЫ

1. **Делай частые коммиты** — лучше 5 маленьких коммитов, чем 1 большой
2. **Пиши понятные сообщения** — через месяц сам поймёшь, что делал
3. **Проверяй перед пушем** — экономит время на исправления
4. **Читай документацию** — там может быть ответ на вопрос
5. **Спрашивай, если не уверен** — лучше спросить, чем сломать

---

## 🎯 ТЕКУЩИЙ СТАТУС ПРОЕКТА

**НЕЙРОПОСТЕР MVP — готов ✅**

### Что работает:
- ✅ Создание постов с медиа
- ✅ Планирование публикаций
- ✅ Автопубликация через n8n
- ✅ Ручная публикация
- ✅ Календарь постов
- ✅ Редактирование/удаление

### Следующие шаги (опционально):
- Drag & drop для изменения порядка фото
- Ресайз изображений до 1080x1350
- Превью поста перед публикацией

---

## 📞 ЕСЛИ ЧТО-ТО ПОШЛО НЕ ТАК

1. **Не паникуй!**
2. **Посмотри ошибку внимательно**
3. **Проверь документацию**
4. **Если не получается — спроси у пользователя**
5. **Если сломал что-то — сразу скажи и исправь**

---

**Помни:** Ты работаешь для заказчиков, которые НЕ знают код, НЕ знают git, НЕ знают терминал. Они работают в двоем с одним аккаунтом Cursor на разных устройствах. Твоя задача — сделать так, чтобы они просто говорили "сделай то-то", а ты делал ВСЁ остальное сам: код, проверки, коммиты, push. Никогда не проси их что-то сделать в терминале или коде! 🚀
