# CLAUDE.md

> AI CITI | НЕЙРОГОРОД — Telegram Mini App для сетевиков.
> MVP НЕЙРОПОСТЕР готов. Заказчики НЕ программисты.

## ROADMAP — ЧИТАЙ ПЕРВЫМ!

**Перед началом работы прочитай:** `docs/ROADMAP_IMPROVEMENTS.md`

Там:
- Список задач с приоритетами
- Кто какую задачу взял
- Правила отметки прогресса

**Когда берёшь задачу:**
1. Отметь в ROADMAP: `🔄 В работе (@Claude)`
2. Сделай задачу
3. Отметь: `✅ Готово (@Claude, дата)`
4. Закоммить ROADMAP вместе с кодом

**Если задача занята другим агентом — НЕ БЕРИ ЕЁ.**

---

## Импорты

Читай эти файлы когда работаешь с соответствующими частями:

- `.claude/rules/core.md` — главные правила (ЧИТАЙ ВСЕГДА)
- `.claude/rules/frontend.md` — React, TypeScript, стили
- `.claude/rules/backend.md` — Supabase, Edge Functions, n8n
- `.claude/rules/database.md` — PostgreSQL, миграции, RLS
- `.claude/memory/decisions.md` — принятые решения
- `.claude/memory/patterns.md` — паттерны проекта

## Быстрый старт

```bash
cat .env.local              # Ключи
npm run build               # Проверка перед коммитом
git add . && git commit -m "feat: ..." && git push  # ВСЕГДА
```

## Стек

```
React 18 + Vite + TypeScript + TailwindCSS + shadcn/ui
Supabase (PostgreSQL + Edge Functions) + n8n
Vercel
```

## Структура

```
src/pages/          — страницы
src/components/     — компоненты (ui/ = shadcn)
src/hooks/          — хуки
src/store/          — Zustand
src/lib/            — утилиты
supabase/migrations/— SQL
supabase/functions/ — Edge Functions
```

## Админы

```
ADMIN_IDS = [643763835, 190202791]
```

---

## 🎯 Автоматическое использование скиллов

**ПРАВИЛО:** При существенных задачах (не мелкие фиксы) ВСЕГДА используй соответствующий скилл.

### UI/UX задачи

| Когда | Скилл | Зачем |
|-------|-------|-------|
| Создаю новый компонент/страницу | `/ui-ux-pro-max` → `/ui-design` | Сначала общие принципы, потом бренд AI CITI |
| Редактирую существующий UI | `/ux-check` | Проверить что не сломал UX |
| Финальная проверка UI | `/ui-visual-validator` | Визуальная валидация |

### React/TypeScript

| Когда | Скилл | Зачем |
|-------|-------|-------|
| Пишу React компонент | `/react-best-practices` | Оптимальные паттерны |
| Сложная логика компонента | `/react-patterns` | Хуки, композиция |
| TypeScript вопросы | `/typescript-expert` | Типы, generics |

### Telegram Mini App

| Когда | Скилл | Зачем |
|-------|-------|-------|
| Работаю с TMA функциями | `/telegram-mini-app` | WebApp API, safe-area, BackButton |
| Работаю с ботом | `/telegram-bot-builder` | Bot API, webhooks |

### Backend/API

| Когда | Скилл | Зачем |
|-------|-------|-------|
| Проектирую API | `/api-design-principles` | REST best practices |
| Авторизация | `/nextjs-supabase-auth` | Auth паттерны |
| Деплой Edge Function | `/supabase-deploy` | Правильный деплой |

### AI/Агенты

| Когда | Скилл | Зачем |
|-------|-------|-------|
| RAG/embeddings | `/rag-engineer` | Vector search, chunking |
| Промпты для AI | `/prompt-engineer` | Оптимальные промпты |
| Автоматизация n8n | `/workflow-automation` | Durable workflows |

### Деплой

| Когда | Скилл | Зачем |
|-------|-------|-------|
| Готов деплоить | `/deploy` | Сборка + проверка + деплой |
| Vercel настройка | `/vercel-deployment` | Env, domains |

### Контент/Маркетинг

| Когда | Скилл | Зачем |
|-------|-------|-------|
| Тексты для UI | `/copywriting` | CTA, заголовки |
| Онбординг | `/onboarding-cro` | Активация пользователей |

### ⚠️ Когда НЕ использовать скиллы

- Мелкие фиксы (опечатка, изменить цвет)
- Простые правки текста
- Удаление/комментирование кода
- Быстрые ответы на вопросы
