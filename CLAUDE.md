# CLAUDE.md — Правила для AI

## 🎯 КОНТЕКСТ

**AI CITI | НЕЙРОГОРОД** — Telegram Mini App для сетевиков.
Текущий модуль: НЕЙРОПОСТЕР (планировщик Instagram).
**MVP готов.** Работаем над улучшениями.

**Заказчики НЕ программисты.** Они работают только через чат. Всё делай сам.

---

## 🚨 GOLDEN RULES (ЧИТАЙ ПЕРВЫМ!)

### 1. ВСЕГДА ПУШЬ В GIT
```bash
git add . && git commit -m "feat: описание" && git push
```
**После КАЖДОГО изменения.** Без исключений. Заказчики работают с разных устройств — если не запушишь, изменения потеряются.

### 2. ДЕЛАЙ ВСЁ САМ
- ❌ НЕ проси заказчика запускать команды
- ❌ НЕ проси проверить код или терминал
- ❌ НЕ объясняй технические детали (если не спросят)
- ✅ Делай сам: код, проверки, коммиты, push
- ✅ Говори просто: "Готово! Добавил кнопку удаления."

### 3. ПРОВЕРЯЙ ПЕРЕД ПУШЕМ
```bash
npm run build  # Должно пройти без ошибок
```
Если ошибки — исправь сам, не говори заказчику.

### 4. НЕ ЛОМАЙ РАБОТАЮЩЕЕ
- Перед изменением — прочитай файл
- Меняй только то, что просят
- Не рефактори "заодно"
- Не добавляй фичи "на будущее"

### 5. ЕСЛИ СЛОМАЛ — ОТКАТИ
```bash
git checkout -- .  # Отменить все незакоммиченные изменения
# или
git revert HEAD    # Отменить последний коммит
```
Сразу скажи заказчику что произошло и что исправил.

---

## 📋 ДЛЯ ЗАКАЗЧИКА: Как давать задачи

### Хорошие примеры:
- "Добавь кнопку удаления поста"
- "Сделай чтобы календарь показывал неделю"
- "Исправь — посты не сохраняются"
- "Покажи что сейчас работает"

### Можно прикладывать:
- Скриншоты ошибок
- Ссылки на страницы
- Примеры как должно выглядеть

### Если что-то не работает:
- Опиши что делал
- Опиши что ожидал
- Опиши что получил
- Приложи скриншот если есть

---

## 🔧 ТЕХНИЧЕСКИЙ СТЕК

```
Frontend:   React 18 + Vite + TypeScript
Стили:      TailwindCSS + shadcn/ui
State:      Zustand + React Query
Backend:    Supabase (PostgreSQL + Edge Functions)
Автоматизация: n8n webhooks
Деплой:     Vercel
```

**НЕ использовать:** Next.js, Express, CSS файлы, другие UI библиотеки.

---

## 🔑 КЛЮЧИ И ДОСТУПЫ

Все ключи в `.env.local` — читай в начале сессии:
```bash
cat .env.local
```

**Никогда не коммить `.env.local` в git!**

---

## 👑 АДМИНЫ

```typescript
const ADMIN_IDS = [643763835, 190202791]
```
Админ-панель `/admin` видна только им.

---

## 📁 СТРУКТУРА

```
src/
├── pages/           # Страницы
├── components/      # Компоненты (ui/ — shadcn)
├── hooks/           # React хуки
├── store/           # Zustand stores
├── lib/             # Утилиты (supabase.ts, utils.ts)
└── types/           # TypeScript типы

supabase/
├── migrations/      # SQL миграции
└── functions/       # Edge Functions
```

---

## 🚨 ЕСЛИ ЧТО-ТО ПОШЛО НЕ ТАК

### Ошибка при build:
1. Прочитай ошибку
2. Исправь сам
3. Снова `npm run build`
4. Только потом коммить

### Конфликт при git pull:
```bash
git stash           # Сохранить свои изменения
git pull            # Получить новые
git stash pop       # Вернуть свои изменения
```
Если конфликт — реши сам, не проси заказчика.

### Сломал продакшен:
1. `git revert HEAD` — откатить последний коммит
2. `git push`
3. Сказать заказчику что произошло
4. Разобраться и исправить нормально

### Не понимаешь задачу:
Спроси! Лучше уточнить, чем сделать не то.

---

## 📚 ДОПОЛНИТЕЛЬНО

- `SKILLS.md` — технические навыки (код, база, стили)
- `docs/ROADMAP.md` — что сделано и что планируется
- `docs/N8N_API.md` — работа с автоматизацией

---

## ✅ ЧЕКЛИСТ ПЕРЕД КАЖДЫМ ОТВЕТОМ

1. [ ] Сделал что просили?
2. [ ] `npm run build` прошёл?
3. [ ] Закоммитил и запушил?
4. [ ] Ответил просто, без технических деталей?
