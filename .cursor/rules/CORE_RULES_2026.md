# 🎯 УЛУЧШЕННЫЕ ПРАВИЛА ДЛЯ АГЕНТОВ | 2026

> **Обновлено:** Январь 2026  
> **Основано на:** Cursor Official Docs, Anthropic Claude Constitution 2026, Best Practices

---

## 📋 СОДЕРЖАНИЕ

1. [Общие принципы](#общие-принципы)
2. [Правила безопасности](#правила-безопасности)
3. [Правила для АНИ (База данных)](#правила-для-ани)
4. [Правила для БОРИ (Backend)](#правила-для-бори)
5. [Правила для ВАСИ (Frontend)](#правила-для-васи)
6. [Workflow и процесс](#workflow-и-процесс)

---

## 🎯 ОБЩИЕ ПРИНЦИПЫ

### 1. ПЛАНИРОВАНИЕ ПЕРЕД КОДИРОВАНИЕМ

**КРИТИЧЕСКИ ВАЖНО:** Всегда используй Plan Mode (`Shift+Tab`) для сложных задач!

**Процесс:**
1. ✅ Исследуй кодовую базу через семантический поиск
2. ✅ Задавай уточняющие вопросы
3. ✅ Создавай детальный план с путями файлов и ссылками на код
4. ✅ Жди одобрения перед началом реализации

**Когда НЕ нужен план:**
- Быстрые изменения (< 5 минут)
- Задачи, которые делал много раз
- Простые правки (исправление опечаток, переименование)

---

### 2. РАЗБИЕНИЕ НА МАЛЕНЬКИЕ ЧАСТИ

**Правило:** Один промт = одна задача = один файл (когда возможно)

**Плохо:**
```
"Добавь авторизацию, интеграцию с Instagram и систему монет"
```

**Хорошо:**
```
1. "Добавь таблицу users в миграцию"
2. "Создай Edge Function для авторизации через Telegram"
3. "Добавь компонент LoginPage"
```

---

### 3. КОНТЕКСТ: ПОЗВОЛЬ АГЕНТУ НАЙТИ САМОМУ

**НЕ делай:**
- ❌ Вручную тегировать все файлы в промте
- ❌ Копировать весь код в промт
- ❌ Перегружать контекст нерелевантными файлами

**ДЕЛАЙ:**
- ✅ Используй семантический поиск: "найди где обрабатывается авторизация"
- ✅ Тегируй только конкретные файлы, если точно знаешь что нужно
- ✅ Используй `@Branch` для контекста о текущей работе
- ✅ Используй `@Past Chats` для ссылок на прошлую работу

---

### 4. GIT: ВСЕГДА ПУШИТЬ ПОСЛЕ ИЗМЕНЕНИЙ

**ОБЯЗАТЕЛЬНО после КАЖДОГО изменения:**

```bash
git add .
git commit -m "feat: описание изменений"
git push
```

**Почему критично:**
- Пользователи работают через чат, не знают git
- Работают с одного аккаунта на разных устройствах
- Если не запушить → изменения потеряются

**НЕ ДЕЛАЙ:** "Сделаю коммит потом" — НЕТ! Сразу после изменений!

---

### 5. ТЕСТИРОВАНИЕ И ПРОВЕРКА

**Перед коммитом ОБЯЗАТЕЛЬНО:**

1. [ ] `npm run build` проходит без ошибок
2. [ ] `npx tsc --noEmit` не показывает ошибок TypeScript
3. [ ] Проверено в браузере (для фронтенда)
4. [ ] SQL синтаксис корректен (для миграций)
5. [ ] RLS политики настроены (для БД)

**Если что-то не проходит → НЕ КОММИТЬ! Исправь сначала!**

---

## 🔒 ПРАВИЛА БЕЗОПАСНОСТИ

### 1. НИКОГДА НЕ ЛОГИРОВАТЬ СЕКРЕТЫ

**КРИТИЧНО:** Удалить все `console.log` с конфигурацией из продакшена!

**Плохо:**
```typescript
console.log('URL:', supabaseUrl)
console.log('Key prefix:', supabaseAnonKey?.substring(0, 30))
```

**Хорошо:**
```typescript
// Только в dev режиме
if (import.meta.env.DEV) {
  console.log('Supabase configured:', !!supabaseUrl)
}
```

---

### 2. ВАЛИДАЦИЯ ENV ПЕРЕМЕННЫХ

**ОБЯЗАТЕЛЬНО:** Проверять наличие обязательных переменных при старте

**Создай:** `src/lib/env.ts`
```typescript
const requiredEnvVars = {
  VITE_SUPABASE_URL: import.meta.env.VITE_SUPABASE_URL,
  VITE_SUPABASE_ANON_KEY: import.meta.env.VITE_SUPABASE_ANON_KEY,
}

for (const [key, value] of Object.entries(requiredEnvVars)) {
  if (!value) {
    throw new Error(`Missing required environment variable: ${key}`)
  }
}
```

---

### 3. НЕТ ХАРДКОДА В КОДЕ

**Плохо:**
```typescript
const supabase = createClient(
  supabaseUrl || 'https://debcwvxlvozjlqkhnauy.supabase.co', // ❌
  supabaseAnonKey || '' // ❌
)
```

**Хорошо:**
```typescript
if (!supabaseUrl || !supabaseAnonKey) {
  throw new Error('Missing Supabase configuration')
}
const supabase = createClient(supabaseUrl, supabaseAnonKey)
```

---

### 4. RLS ВСЕГДА ВКЛЮЧЕН

**Правило:** Каждая таблица ДОЛЖНА иметь RLS политики!

```sql
ALTER TABLE table_name ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users can manage own data" ON table_name
  FOR ALL USING (user_id = auth.uid());
```

---

## 🗄️ ПРАВИЛА ДЛЯ АНИ (База данных)

### Твоя роль
Ты отвечаешь за ВСЮ работу с базой данных Supabase.

### Твои файлы
```
supabase/migrations/*.sql
```

### ⛔ НЕ ТРОГАЙ
- `src/**/*` (это ВАСЯ)
- `supabase/functions/*` (это БОРЯ)

---

### Правила SQL

**Именование:**
- Таблицы: `snake_case` множественное число (`users`, `scheduled_posts`)
- Колонки: `snake_case` (`created_at`, `user_id`)
- Индексы: `idx_tablename_columns`
- Политики: понятное описание на русском

**Обязательные колонки:**
```sql
id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
created_at TIMESTAMPTZ DEFAULT NOW(),
updated_at TIMESTAMPTZ DEFAULT NOW()
```

**RLS — ВСЕГДА!**
```sql
ALTER TABLE table_name ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Description" ON table_name
  FOR ALL USING (user_id = auth.uid());
```

---

### Шаблон миграции

```sql
-- supabase/migrations/XXX_description.sql

-- ============================================
-- Описание: Что делает эта миграция
-- Автор: АНЯ
-- Дата: YYYY-MM-DD
-- ============================================

-- Создание таблицы
CREATE TABLE IF NOT EXISTS table_name (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  -- колонки
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- Индексы
CREATE INDEX IF NOT EXISTS idx_table_column ON table_name(column);

-- RLS
ALTER TABLE table_name ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Description" ON table_name
  FOR ALL USING (user_id = auth.uid());

-- Триггер updated_at
CREATE TRIGGER update_table_updated_at
  BEFORE UPDATE ON table_name
  FOR EACH ROW EXECUTE FUNCTION update_updated_at();
```

---

### Проверка перед коммитом

1. [ ] SQL синтаксис корректный
2. [ ] RLS политики настроены
3. [ ] Индексы на часто используемые колонки
4. [ ] Внешние ключи с ON DELETE CASCADE
5. [ ] Нумерация миграции правильная (001, 002, ...)
6. [ ] **ОБЯЗАТЕЛЬНО: `git add . && git commit -m "..." && git push`**

---

## 🔧 ПРАВИЛА ДЛЯ БОРИ (Backend)

### Твоя роль
Ты отвечаешь за ВСЕ Edge Functions и серверную логику.

### Твои файлы
```
supabase/functions/*/index.ts
```

### ⛔ НЕ ТРОГАЙ
- `src/**/*` (это ВАСЯ)
- `supabase/migrations/*` (это АНЯ)

---

### Технологии

- **Runtime:** Deno (Edge Functions)
- **База:** Supabase Client
- **API:** Instagram Graph API, Telegram Bot API
- **N8N:** Автоматизация workflow (см. `/docs/N8N_API.md`)

---

### Шаблон Edge Function

```typescript
// supabase/functions/function-name/index.ts

import { serve } from 'https://deno.land/std@0.168.0/http/server.ts'
import { createClient } from 'https://esm.sh/@supabase/supabase-js@2'

const corsHeaders = {
  'Access-Control-Allow-Origin': '*',
  'Access-Control-Allow-Headers': 'authorization, x-client-info, apikey, content-type',
}

serve(async (req) => {
  // CORS preflight
  if (req.method === 'OPTIONS') {
    return new Response('ok', { headers: corsHeaders })
  }

  try {
    // Создать Supabase клиент
    const supabase = createClient(
      Deno.env.get('SUPABASE_URL')!,
      Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!
    )

    // Твоя логика здесь
    const { data, error } = await supabase
      .from('table')
      .select('*')

    if (error) throw error

    return new Response(JSON.stringify({ data }), {
      headers: { ...corsHeaders, 'Content-Type': 'application/json' },
      status: 200
    })

  } catch (error) {
    return new Response(JSON.stringify({ error: error.message }), {
      headers: { ...corsHeaders, 'Content-Type': 'application/json' },
      status: 500
    })
  }
})
```

---

### Проверка перед коммитом

1. [ ] CORS headers добавлены
2. [ ] Ошибки обрабатываются try/catch
3. [ ] Используется service_role_key для админских операций
4. [ ] Секреты берутся из Deno.env
5. [ ] **ОБЯЗАТЕЛЬНО: `git add . && git commit -m "..." && git push`**

---

## 🎨 ПРАВИЛА ДЛЯ ВАСИ (Frontend)

### Твоя роль
Ты отвечаешь за ВСЮ фронтенд часть: React компоненты, страницы, стили.

### Твои файлы
```
src/**/*.tsx
src/**/*.ts
src/**/*.css
public/**/*
index.html
```

### ⛔ НЕ ТРОГАЙ
- `supabase/migrations/*` (это АНЯ)
- `supabase/functions/*` (это БОРЯ)

---

### Технологии

- **React 19** + TypeScript
- **Vite** (сборщик)
- **TailwindCSS** (стили)
- **shadcn/ui** (UI компоненты)
- **React Router 7** (роутинг)
- **Zustand** (стейт менеджмент)
- **@tanstack/react-query** (кэширование запросов)

---

### Структура компонентов

```
src/
├── pages/                 # Страницы (роуты)
│   ├── Home.tsx
│   ├── Profile.tsx
│   └── tools/
│       └── poster/
│           ├── index.tsx      # Дашборд
│           ├── create.tsx     # Создание поста
│           └── calendar.tsx    # Календарь
│
├── components/
│   ├── ui/               # shadcn/ui (Button, Card, Input...)
│   ├── layout/           # Header, Footer, Navigation
│   └── poster/           # Компоненты НЕЙРОПОСТЕР
│
├── hooks/                # Кастомные хуки
├── store/                # Zustand stores
├── lib/                  # Утилиты
└── types/                # TypeScript типы
```

---

### Стили

**Цветовая схема:**
```css
--background: #0a0a0a;      /* Тёмный фон */
--foreground: #ffffff;      /* Белый текст */
--primary: #FF5A1F;         /* Оранжевый */
--primary-hover: #FF8A3D;
--card: rgba(255,255,255,0.1);
--border: rgba(255,255,255,0.2);
```

**Glassmorphism (используй везде для карточек):**
```tsx
<div className="bg-white/10 backdrop-blur-md border border-white/20 rounded-xl">
```

**Safe area для Telegram:**
```tsx
// Минимальный отступ: safe-area + 56px для кнопок Telegram
<div style={{ 
  paddingTop: `calc(env(safe-area-inset-top, 0px) + 56px)` 
}}>
  {/* Контент */}
</div>
```

---

### Проверка перед коммитом

1. [ ] `npm run build` без ошибок
2. [ ] TypeScript ошибок нет (`npx tsc --noEmit`)
3. [ ] Компоненты типизированы
4. [ ] Используется только Tailwind (нет inline styles)
5. [ ] Проверено в браузере
6. [ ] **ОБЯЗАТЕЛЬНО: `git add . && git commit -m "..." && git push`**

---

## 🔄 WORKFLOW И ПРОЦЕСС

### Когда начинать новую беседу

**Начинай новую беседу когда:**
- Переходишь к другой задаче или фиче
- Агент кажется запутанным или повторяет ошибки
- Завершил одну логическую единицу работы

**Продолжай беседу когда:**
- Итерируешь над той же фичей
- Агенту нужен контекст из начала обсуждения
- Отлаживаешь что-то, что только что построил

**Длинные беседы:** После многих поворотов контекст накапливает шум. Если агент теряет фокус → начинай новую беседу.

---

### Использование Skills и Subagents

**Skills** (`.cursor/skills/`):
- Для динамических возможностей
- Процедурные инструкции "как делать"
- Используются агентом когда релевантны

**Subagents**:
- Для параллельной работы
- Специализированные агенты для конкретных задач
- Используются автоматически когда нужно

**Rules** (`.cursor/rules/`):
- Для статического контекста
- Всегда активны
- Документируют паттерны и стандарты проекта

---

### План действий для сложных задач

1. **Исследование** → Используй семантический поиск
2. **Планирование** → Создай план в Plan Mode
3. **Реализация** → Разбивай на маленькие части
4. **Тестирование** → Проверяй перед коммитом
5. **Коммит** → Всегда пушить в git

---

## ✅ ЧЕКЛИСТ ПЕРЕД КОММИТОМ (ОБЯЗАТЕЛЬНО!)

1. [ ] Код написан и соответствует требованиям
2. [ ] TypeScript ошибок нет (`npm run build` проходит)
3. [ ] Изменения закоммичены в git (`git add . && git commit -m "..." && git push`)
4. [ ] Деплой прошел успешно (Vercel автоматически)
5. [ ] Для визуальных/механических: проверено через Playwright/браузер

---

## 📚 РЕСУРСЫ

- [Cursor Official Docs](https://cursor.com/docs)
- [Cursor Best Practices Blog](https://cursor.com/blog/agent-best-practices)
- [Anthropic Claude Constitution 2026](https://anthropic.com/news/claude-new-constitution)
- [Awesome Cursor Rules](https://github.com/PatrickJS/awesome-cursorrules)

---

**Последнее обновление:** Январь 2026
