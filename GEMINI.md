# GEMINI.md

> **AI CITI | НЕЙРОГОРОД** — Telegram Mini App для сетевиков.
> MVP НЕЙРОПОСТЕР готов. Заказчики НЕ программисты.

---

## 🚨 КРИТИЧЕСКИЕ ПРАВИЛА (НАРУШЕНИЕ = БАГ)

### 1. ВСЕГДА ПУШИ
```bash
git add . && git commit -m "тип: описание" && git push
```
После КАЖДОГО изменения. Без исключений.

### 2. ВСЕГДА ПРОВЕРЯЙ ПЕРЕД КОММИТОМ
```bash
npm run build
```
Сломано → исправь сам → только потом коммить.

### 3. ДЕЛАЙ ВСЁ САМ
Заказчики НЕ знают git, npm, терминал. **Никогда не проси их:**
- запускать команды
- проверять код
- смотреть логи

**Говори просто:** "Готово! Добавил кнопку." / "Исправил баг."

### 4. НЕ ЛОМАЙ
- Прочитай файл ПЕРЕД изменением
- Меняй ТОЛЬКО то, что просят
- НЕ рефактори "заодно"
- НЕ добавляй "на будущее"

---

## 🧠 АЛГОРИТМ РАБОТЫ (Chain-of-Thought)

При получении задачи выполняй по шагам:

```
ШАГИ ДЛЯ ЛЮБОЙ ЗАДАЧИ:

1. ПОНЯТЬ
   └─ Что именно просят? Переформулируй своими словами.

2. НАЙТИ
   └─ Какие файлы затронуты? Прочитай их.

3. ПРОВЕРИТЬ KNOWLEDGE (только Antigravity)
   └─ Есть ли KI по этой теме?
   └─ ~/.gemini/antigravity/knowledge/

4. ВЫБРАТЬ СКИЛЛ (если задача существенная)
   └─ UI? → ui-ux-pro-max
   └─ React? → react-best-practices / react-patterns
   └─ TMA? → telegram-mini-app
   └─ API? → api-design-principles
   └─ n8n? → workflow-automation
   └─ Промпты? → prompt-engineer

5. СДЕЛАТЬ
   └─ Минимальные изменения для решения задачи.

6. ПРОВЕРИТЬ
   └─ npm run build — работает?
   └─ Self-check (см. ниже)

7. ЗАКОММИТИТЬ
   └─ git add . && git commit -m "тип: описание" && git push
```

---

## ✅ SELF-CHECK (после каждого изменения)

### После UI изменений:
```
☐ Использовал оранжевый (#FF5A1F) для CTA?
☐ Использовал бирюзовый (#06B6D4) для вторичных?
☐ НЕ использовал фиолетовый/розовый/синий?
☐ Добавил hover/active состояния?
☐ TMA safe-area учтена?
```

### После React изменений:
```
☐ Функциональный компонент (не классовый)?
☐ TypeScript типы указаны?
☐ Нет any?
☐ React Query для данных?
☐ Zustand для глобального стейта?
```

### После Backend изменений:
```
☐ CORS headers добавлены?
☐ Error handling есть?
☐ RLS политики обновлены?
```

### После n8n изменений:
```
☐ Workflow сохранён (не только в редакторе)?
☐ Тестовый запуск прошёл?
☐ Error handling / fallback есть?
```

### Перед каждым коммитом:
```
☐ npm run build проходит?
☐ Изменил только то, что просили?
☐ Не добавил лишнего?
☐ Коммит сообщение понятное?
```

---

## 🎯 КОГДА КАКОЙ СКИЛЛ ИСПОЛЬЗОВАТЬ

### Decision Tree:

```
Задача связана с...

UI/UX?
├─ Новый компонент/страница → ui-ux-pro-max
├─ Редактирование UI → ui-ux-designer
└─ Визуальная проверка → ui-visual-validator

React/TypeScript?
├─ Компонент → react-best-practices
├─ Сложная логика → react-patterns
└─ Типы → typescript-expert

Telegram?
├─ Mini App → telegram-mini-app
└─ Бот → telegram-bot-builder

Backend?
├─ API дизайн → api-design-principles
├─ Auth → nextjs-supabase-auth
└─ Backend patterns → cc-skill-backend-patterns

AI/Агенты?
├─ RAG → rag-engineer
├─ Промпты → prompt-engineer
├─ n8n workflow → workflow-automation
├─ Агенты → autonomous-agent-patterns
└─ Инструменты → agent-tool-builder

Деплой?
└─ Vercel → vercel-deployment

Контент?
├─ Тексты → copywriting
├─ Маркетинг → marketing-ideas
└─ Контент-план → content-creator

Аналитика?
├─ Трекинг → analytics-tracking
├─ KPI → business-analyst
└─ Конкуренты → competitive-landscape
```

### ⚠️ НЕ используй скиллы для:
- Фикс опечатки
- Изменить один цвет
- Удалить строку
- Ответить на вопрос

---

## 🎨 ДИЗАЙН-СИСТЕМА (СТРОГО!)

### Цвета — запомни навсегда:

| Роль | Цвет | Tailwind | Когда |
|------|------|----------|-------|
| **Primary** | `#FF5A1F` | `orange-500`, `from-orange-400 to-orange-500` | CTA кнопки, акценты |
| **Secondary** | `#06B6D4` | `cyan-500`, `from-cyan-400 to-cyan-500` | AI иконки, вторичные |
| **Background** | `#FFFFFF` / `#FFF8F5` | `bg-white`, `bg-[#FFF8F5]` | Фон |
| **Text** | `#1A1A1A` | `text-gray-900` | Основной текст |

### 🚫 ЗАПРЕЩЁННЫЕ ЦВЕТА:
```
❌ purple — НИКОГДА
❌ pink — НИКОГДА
❌ blue (кроме info статусов) — НИКОГДА
```

### Glassmorphism:
```tsx
className="bg-white/80 backdrop-blur-xl border border-white/60 rounded-2xl shadow-sm"
```

### CTA кнопка:
```tsx
className="bg-gradient-to-r from-orange-400 to-orange-500 text-white rounded-xl px-4 py-2 hover:shadow-lg transition-all duration-200"
```

---

## 📁 СТРУКТУРА ПРОЕКТА

```
src/
├── pages/          # Страницы (роуты)
├── components/     # Компоненты
│   └── ui/         # shadcn/ui
├── hooks/          # React хуки
├── store/          # Zustand stores
├── lib/            # Утилиты
└── types/          # TypeScript типы

supabase/
├── migrations/     # SQL миграции
└── functions/      # Edge Functions (Deno)
```

---

## 🔧 СТЕК

```
Frontend: React 18 + Vite + TypeScript + TailwindCSS + shadcn/ui
Backend:  Supabase (PostgreSQL + Edge Functions) + n8n
Deploy:   Vercel
AI:       Gemini / Vertex AI
Agents:   n8n + Mastra (experimental)
```

---

## 📦 KNOWLEDGE ITEMS (только для Antigravity)

Перед началом сложной задачи проверь KI:

| Тема | KI |
|------|---|
| Карусели | `carousel_content_generation_system` |
| Платформа | `ai_citi_platform_architecture` |
| Экономика | `ai_citi_neuron_economy` |
| n8n | `n8n_process_automation` |
| TMA | `telegram_mini_app_development` |
| Платежи | `payment_systems_integration` |

Путь: `~/.gemini/antigravity/knowledge/`

---

## ❌ АНТИПАТТЕРНЫ (НИКОГДА не делай)

### Код:
```tsx
// ❌ ПЛОХО
style={{ backgroundColor: 'purple' }}  // inline styles + запрещённый цвет
const data: any = await fetch(...)      // any
class MyComponent extends Component {}   // классовый компонент

// ✅ ХОРОШО
className="bg-orange-500"               // Tailwind + правильный цвет
const data: Post[] = await fetch(...)   // типизация
function MyComponent() {}               // функциональный
```

### Git:
```bash
# ❌ ПЛОХО
git commit -m "fix"           # непонятно что
git add .env.local            # секреты в git

# ✅ ХОРОШО
git commit -m "fix: исправил сохранение постов"
# .env.local в .gitignore
```

### Формат коммитов:
```
feat: новая функциональность
fix: исправление бага
refactor: рефакторинг без изменения поведения
style: форматирование, стили
docs: документация
chore: рутинные задачи
```

### Общение:
```
# ❌ ПЛОХО
"Запусти npm run build и скинь мне логи"
"Проверь в терминале..."

# ✅ ХОРОШО
"Готово! Кнопка теперь работает."
"Исправил. Посты сохраняются."
```

---

## 🔒 БЕЗОПАСНОСТЬ

- `.env.local` — НИКОГДА в git
- Service Role Key — ТОЛЬКО на сервере
- RLS — на КАЖДОЙ таблице
- Валидация — на ВСЕХ inputs

---

## 👥 АДМИНЫ

```typescript
const ADMIN_IDS = [643763835, 190202791]
```

---

## 🔄 ОТКАТ (если сломал)

```bash
# Незакоммиченные изменения
git checkout -- .

# Последний коммит
git revert HEAD && git push
```

Скажи заказчику что произошло (простыми словами).

---

## 📋 ROADMAP

**Перед началом работы:** `docs/ROADMAP_IMPROVEMENTS.md`

Когда берёшь задачу:
1. Отметь: `🔄 В работе (@Antigravity)`
2. Сделай задачу
3. Отметь: `✅ Готово (@Antigravity, дата)`
4. Закоммить ROADMAP вместе с кодом

**Если задача занята — НЕ БЕРИ.**
