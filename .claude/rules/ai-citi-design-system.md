# AI CITI Design System

> **КРИТИЧЕСКИ ВАЖНО:** Всегда используй эти цвета и правила при работе с UI/UX!

## 🎨 ЦВЕТОВАЯ ПАЛИТРА (СТРОГО!)

### Основные цвета бренда:

| Роль | Hex | Tailwind классы | Использование |
|------|-----|-----------------|---------------|
| **Primary (Оранжевый)** | `#FF5A1F` | `from-orange-400 to-orange-500`, `bg-orange-500` | CTA кнопки, акценты, градиенты |
| **Primary Light** | `#FF8A3D` | `orange-400` | Hover states, светлые акценты |
| **Primary Dark** | `#E54A10` | `orange-600` | Активные состояния |
| **Secondary (Бирюзовый)** | `#06B6D4` | `from-cyan-400 to-cyan-500`, `bg-cyan-500` | Вторичные акценты, иконки |
| **Secondary Light** | `#22D3EE` | `cyan-400` | Светлые бирюзовые элементы |
| **Background** | `#FFFFFF` | `bg-white` | Основной фон |
| **Background Soft** | `#FFF8F5` | `bg-[#FFF8F5]` | Мягкий фон (peach/cream) |
| **Text Primary** | `#1A1A1A` | `text-gray-900` | Основной текст |
| **Text Secondary** | `#666666` | `text-gray-600` | Второстепенный текст |

### Градиенты:

```css
/* Оранжевый градиент (Primary) */
from-orange-400 to-orange-500
bg-gradient-to-r from-orange-400 to-orange-500

/* Бирюзовый градиент (Secondary) */
from-cyan-400 to-cyan-500
bg-gradient-to-br from-cyan-400 to-cyan-500

/* Мягкий фон */
from-[#FFF8F5] via-white to-[#FFF8F5]
```

### Статусные цвета:

- **Success**: `#10B981` (green-500)
- **Warning**: `#F59E0B` (amber-500)
- **Error**: `#EF4444` (red-500)
- **Info**: `#3B82F6` (blue-500)

---

## 🚫 ЧТО НЕ ИСПОЛЬЗОВАТЬ:

- ❌ **Фиолетовый** (`#7C3AED`, `purple-500`) — НЕ наш цвет!
- ❌ **Синий** (`#2563EB`, `blue-600`) — только для info статусов
- ❌ **Розовый** (`#EC4899`, `pink-500`) — НЕ использовать
- ❌ Другие цвета кроме оранжевого и бирюзового для основных элементов

---

## 🎯 ПРАВИЛА ПРИМЕНЕНИЯ:

### Primary (Оранжевый) используется для:
- ✅ CTA кнопки (купить, отправить, сохранить)
- ✅ Активные табы
- ✅ Важные акценты
- ✅ Градиенты на карточках
- ✅ Hover states на важных элементах

### Secondary (Бирюзовый) используется для:
- ✅ Иконки AI ассистента
- ✅ Вторичные кнопки
- ✅ Информационные элементы
- ✅ Декоративные элементы
- ✅ Пьедесталы, карточки персонажей

### Комбинации:
- ✅ Оранжевый + Бирюзовый — основная комбинация
- ✅ Оранжевый + Белый — для CTA
- ✅ Бирюзовый + Белый — для информационных карточек

---

## 📐 ТИПОГРАФИКА:

- **Заголовки**: `font-bold`, `text-gray-900` или `text-[#1A1A1A]`
- **Основной текст**: `text-gray-700` или `text-gray-600`
- **Второстепенный**: `text-gray-500`
- **Акценты**: `text-orange-500` или `text-cyan-600`

---

## 🎨 СТИЛЬ КАРТОЧЕК:

### Glassmorphism (основной стиль):
```tsx
className="bg-white/80 backdrop-blur-xl border border-white/60 rounded-2xl shadow-sm"
```

### Градиентные карточки:
```tsx
// Оранжевые
className="bg-gradient-to-r from-orange-400 to-orange-500 rounded-2xl"

// Бирюзовые
className="bg-gradient-to-br from-cyan-400 to-cyan-500 rounded-2xl"
```

---

## ⚡ АНИМАЦИИ И TRANSITIONS:

- **Duration**: `200-300ms` (не больше!)
- **Easing**: `ease-in-out` или `[0.4, 0, 0.2, 1]`
- **Hover**: `hover:scale-[1.02]` или `hover:shadow-lg`
- **Active**: `active:scale-[0.98]`

---

## ✅ ЧЕКЛИСТ ПЕРЕД ИСПОЛЬЗОВАНИЕМ UI/UX PRO MAX:

Когда используешь UI/UX Pro Max скилл:

1. ✅ **Сгенерируй дизайн-систему** через скилл
2. ✅ **ЗАМЕНИ цвета** на оранжевый/бирюзовый из этого файла
3. ✅ **Сохрани остальные рекомендации** (spacing, typography, effects)
4. ✅ **Примени правила** из этого файла

**Пример:**
```
UI/UX Pro Max рекомендует: Primary #7C3AED (фиолетовый)
→ ЗАМЕНИ на: Primary #FF5A1F (оранжевый)
→ Остальное (spacing, typography, effects) оставь как рекомендовано
```

---

## 📝 ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ:

### Кнопка CTA:
```tsx
<button className="bg-gradient-to-r from-orange-400 to-orange-500 text-white rounded-xl px-4 py-2 hover:shadow-lg transition-all duration-200 cursor-pointer">
  Купить
</button>
```

### Карточка с бирюзовым акцентом:
```tsx
<div className="bg-white border border-cyan-200 rounded-2xl p-4">
  <div className="w-10 h-10 bg-gradient-to-br from-cyan-400 to-cyan-500 rounded-full" />
</div>
```

### Таб активный:
```tsx
<button className="bg-white text-orange-500 shadow-sm">
  Активный таб
</button>
```

---

## 🎯 ГЛАВНОЕ ПРАВИЛО:

**ВСЕГДА используй оранжевый и бирюзовый как основные цвета!**
**НИКОГДА не используй фиолетовый, розовый или другие цвета для основных элементов!**
