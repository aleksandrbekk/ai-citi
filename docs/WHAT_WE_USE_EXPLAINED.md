# 🔍 ЧТО МЫ ИСПОЛЬЗУЕМ - ОБЪЯСНЕНИЕ

## ❓ ЧТО ТАКОЕ "GenAI App Builder"?

**GenAI App Builder** (теперь называется **Vertex AI Search**) — это **НЕ модели Gemini**.

Это **отдельный сервис** для создания поисковых приложений (RAG - Retrieval Augmented Generation):
- Поиск по документам с помощью AI
- Создание чатботов для поиска информации
- Работа с корпоративными данными

**Это НЕ то, что мы используем!**

---

## ✅ ЧТО МЫ РЕАЛЬНО ИСПОЛЬЗУЕМ

### **Vertex AI API** для вызова моделей Gemini

**Код в `supabase/functions/gemini-chat/index.ts`:**

```typescript
// Строка 282
const endpoint = `https://${LOCATION}-aiplatform.googleapis.com/v1/projects/${PROJECT_ID}/locations/${LOCATION}/publishers/google/models/${model}:generateContent`
```

**Это:**
- ✅ **Vertex AI API** (`aiplatform.googleapis.com`)
- ✅ Вызов моделей **Gemini** напрямую (`gemini-2.5-flash`, `gemini-2.5-pro`)
- ✅ Endpoint: `generateContent` для генерации текста

**Это НЕ GenAI App Builder!**

---

## 💰 КРЕДИТЫ И ИХ ПРИМЕНЕНИЕ

### 1. **"Trial credit for GenAI App Builder" (฿32,310.50)**
- ❌ **НЕ применяется** к Vertex AI API (`aiplatform.googleapis.com`)
- ✅ Применяется только к **GenAI App Builder** (Vertex AI Search)
- **Мы НЕ используем GenAI App Builder**, поэтому кредит не списывается

### 2. **"Dialogflow CX Trial" (฿18,980.41)**
- ❌ **НЕ применяется** к Vertex AI API
- ✅ Применяется только к **Dialogflow CX** (чатботы)
- **Мы НЕ используем Dialogflow**, поэтому кредит не списывается

### 3. **$300 Welcome Credit**
- ✅ **ДОЛЖЕН применяться** к Vertex AI API
- ⚠️ Может быть использован или истёк

---

## 🔍 КАК ПРОВЕРИТЬ В КОНСОЛИ БИЛЛИНГА

**URL:** https://console.cloud.google.com/billing/01DB30-E9BEE6-19F55A/reports?project=gen-lang-client-0102901194

### Что проверить:

1. **Открой "Cost table" (Таблица расходов)**
2. **Фильтр по сервису:**
   - Ищи: `aiplatform.googleapis.com`
   - Или: `Vertex AI API`
3. **Проверь расходы:**
   - Есть ли расходы на `aiplatform.googleapis.com`?
   - Какие кредиты применяются?
   - Есть ли $300 Welcome Credit?

4. **Проверь использование:**
   - Открой "Usage" (Использование)
   - Фильтр: `aiplatform.googleapis.com`
   - Посмотри количество запросов

---

## 📊 СРАВНЕНИЕ

| Сервис | Что это | Используем? |
|--------|---------|-------------|
| **Vertex AI API** (`aiplatform.googleapis.com`) | Вызов моделей Gemini напрямую | ✅ **ДА** |
| **GenAI App Builder** (Vertex AI Search) | Платформа для поисковых приложений | ❌ НЕТ |
| **Dialogflow CX** | Платформа для чатботов | ❌ НЕТ |

---

## ✅ ИТОГ

**Мы используем:**
- ✅ **Vertex AI API** для вызова моделей Gemini
- ✅ Endpoint: `aiplatform.googleapis.com/v1/.../generateContent`
- ✅ Модели: `gemini-2.5-flash`, `gemini-2.5-pro`

**Кредиты:**
- ❌ "GenAI App Builder" — не применяется (мы не используем этот сервис)
- ❌ "Dialogflow CX" — не применяется (мы не используем этот сервис)
- ✅ $300 Welcome Credit — должен применяться (проверить в консоли)

**Почему кредиты не списываются:**
- Используются **специфичные кредиты** для других сервисов
- **$300 Welcome Credit** может быть использован или истёк
