# 📋 ЗАДАЧИ: Настройка и тестирование RAG агента

**Дата:** 2026-01-27  
**Статус:** RAG код готов, нужно настроить и протестировать  
**Цель:** Проверить списание кредитов GenAI App Builder (฿32,310.50)

---

## ✅ ЧТО УЖЕ СДЕЛАНО

1. ✅ Добавлена функция `searchRAG()` в `supabase/functions/gemini-chat/index.ts`
2. ✅ Добавлена поддержка параметров `useRAG` и `ragDataStoreId`
3. ✅ Реализован гибридный режим (RAG + Gemini)
4. ✅ Код закоммичен и запушен в git

---

## 🎯 ЗАДАЧИ ДЛЯ НОВОГО ЧАТА

### **ЗАДАЧА 1: Создать Data Store в GenAI App Builder**

**Что нужно сделать:**
1. Открыть консоль GenAI App Builder:
   ```
   https://console.cloud.google.com/gen-app-builder/data-stores?project=gen-lang-client-0102901194
   ```

2. Создать Data Store:
   - **Display name:** `test-rag-store` (или любое имя)
   - **Data store type:** Website / Cloud Storage / PDF
   - **Location:** `global` (обязательно!)

3. Загрузить тестовые документы:
   - Можно загрузить простой текстовый файл с тестовым контентом
   - Или указать URL сайта
   - Или загрузить PDF

4. Дождаться индексации (может занять несколько минут)

5. **Скопировать Data Store ID:**
   - После создания, в URL будет что-то вроде:
   ```
   https://console.cloud.google.com/gen-app-builder/data-stores/1234567890?project=gen-lang-client-0102901194
   ```
   - Data Store ID = `1234567890`

**Результат:** Data Store создан, проиндексирован, ID скопирован

---

### **ЗАДАЧА 2: Проверить и настроить права Service Account**

**Что нужно сделать:**

1. Проверить текущие права:
   ```bash
   gcloud projects get-iam-policy gen-lang-client-0102901194 \
     --flatten="bindings[].members" \
     --filter="bindings.members:ai-citi-assistant@gen-lang-client-0102901194.iam.gserviceaccount.com" \
     --format="table(bindings.role)"
   ```

2. Добавить роль для Discovery Engine API:
   ```bash
   gcloud projects add-iam-policy-binding gen-lang-client-0102901194 \
     --member="serviceAccount:ai-citi-assistant@gen-lang-client-0102901194.iam.gserviceaccount.com" \
     --role="roles/discoveryengine.viewer"
   ```

   Или через консоль:
   - https://console.cloud.google.com/iam-admin/iam?project=gen-lang-client-0102901194
   - Найти `ai-citi-assistant@gen-lang-client-0102901194.iam.gserviceaccount.com`
   - Добавить роль: `Discovery Engine Viewer` или `Discovery Engine Admin`

**Результат:** Service Account имеет права на Discovery Engine API

---

### **ЗАДАЧА 3: Включить Discovery Engine API**

**Что нужно сделать:**

1. Проверить, включен ли API:
   ```bash
   gcloud services list --enabled --project=gen-lang-client-0102901194 --filter="name:discoveryengine.googleapis.com"
   ```

2. Если не включен, включить:
   ```bash
   gcloud services enable discoveryengine.googleapis.com --project=gen-lang-client-0102901194
   ```

   Или через консоль:
   - https://console.cloud.google.com/apis/library/discoveryengine.googleapis.com?project=gen-lang-client-0102901194
   - Нажать "Enable"

**Результат:** Discovery Engine API включен

---

### **ЗАДАЧА 4: Протестировать RAG через API**

**Что нужно сделать:**

1. Получить Supabase URL и Anon Key из `.env.local`:
   ```bash
   cat .env.local | grep SUPABASE
   ```

2. Сделать тестовый запрос через curl:
   ```bash
   curl -X POST https://YOUR_SUPABASE_URL/functions/v1/gemini-chat \
     -H "Authorization: Bearer YOUR_ANON_KEY" \
     -H "Content-Type: application/json" \
     -d '{
       "message": "Тестовый вопрос для RAG",
       "userId": "test-user-123",
       "useRAG": true,
       "ragDataStoreId": "YOUR_DATA_STORE_ID"
     }'
   ```

3. Проверить ответ:
   - Должен вернуться JSON с полем `rag.used: true`
   - Должны быть источники в `rag.sources`
   - Не должно быть ошибок

**Результат:** RAG работает, возвращает ответы

---

### **ЗАДАЧА 5: Проверить списание кредитов**

**Что нужно сделать:**

1. Открыть консоль биллинга:
   ```
   https://console.cloud.google.com/billing/01DB30-E9BEE6-19F55A/reports?project=gen-lang-client-0102901194
   ```

2. Проверить расходы:
   - Фильтр: `discoveryengine.googleapis.com`
   - Проверить, есть ли расходы на Discovery Engine API
   - Проверить, списывается ли кредит "Trial credit for GenAI App Builder" (฿32,310.50)

3. Сделать несколько тестовых запросов (5-10) и проверить изменения

**Результат:** Подтверждено, что кредиты списываются

---

### **ЗАДАЧА 6: Проверить логи Edge Function**

**Что нужно сделать:**

1. Открыть Supabase Dashboard:
   - Edge Functions → `gemini-chat` → Logs

2. Проверить логи после тестового запроса:
   - Должны быть логи: `RAG search: query="..."`, `RAG result: ...`
   - Не должно быть ошибок типа `RAG search error`
   - Должны быть логи успешных запросов

**Результат:** Логи показывают успешную работу RAG

---

## 📝 ВАЖНЫЕ ЗАМЕЧАНИЯ

### **Критичные моменты:**

1. **Локация Data Store:** Должна быть `global` (не `us-central1`!)
   - Endpoint: `https://discoveryengine.googleapis.com/v1/projects/.../locations/global/...`

2. **Service Account:** Должен иметь роль `discoveryengine.viewer` или `discoveryengine.admin`

3. **API:** Discovery Engine API должен быть включен

4. **Бесплатный лимит:** Первые 10,000 запросов в месяц бесплатно

---

## 🔍 ПРОВЕРКА УСПЕХА

### **Критерии успешного теста:**

- ✅ Data Store создан и проиндексирован
- ✅ Service Account имеет права
- ✅ Discovery Engine API включен
- ✅ RAG запросы работают (возвращают ответы)
- ✅ В логах нет ошибок
- ✅ В консоли биллинга видны расходы на `discoveryengine.googleapis.com`
- ✅ Кредит "GenAI App Builder" списывается

---

## 🚨 ВОЗМОЖНЫЕ ПРОБЛЕМЫ

### **Проблема 1: "Data Store not found"**
- Проверить правильность Data Store ID
- Убедиться, что Data Store создан в локации `global`

### **Проблема 2: "Permission denied"**
- Проверить права Service Account
- Добавить роль `discoveryengine.viewer`

### **Проблема 3: "API not enabled"**
- Включить Discovery Engine API

### **Проблема 4: "RAG не нашел ответ"**
- Загрузить документы в Data Store
- Подождать индексации
- Попробовать другой вопрос

---

## 📁 СВЯЗАННЫЕ ДОКУМЕНТЫ

- `/docs/RAG_TEST_INSTRUCTIONS.md` - подробная инструкция по тестированию
- `/docs/RAG_INTEGRATION_PLAN.md` - план интеграции RAG
- `/docs/RAG_BILLING_EXPLANATION.md` - объяснение списания кредитов
- `/supabase/functions/gemini-chat/index.ts` - код с функцией `searchRAG()`

---

## 🎯 NEXT STEPS ПОСЛЕ УСПЕШНОГО ТЕСТА

1. Добавить UI для переключения RAG режима в чате
2. Сохранить Data Store ID в настройках (БД)
3. Добавить мониторинг использования RAG
4. Оптимизировать промпты для лучших результатов
5. Создать несколько Data Stores для разных проектов (SystemA, AR Arena, etc.)

---

## 💡 ПОДСКАЗКИ ДЛЯ НОВОГО ЧАТА

1. **Используй браузер** для открытия консоли Google Cloud (у тебя есть доступ)
2. **Проверяй логи** в Supabase Dashboard после каждого шага
3. **Тестируй постепенно** - сначала создай Data Store, потом проверь права, потом тестируй API
4. **Документируй результаты** - записывай что работает, что нет
5. **Если что-то не работает** - проверь логи и ошибки, они подскажут проблему

---

**Главное:** Цель - проверить, что кредиты GenAI App Builder (฿32,310.50) списываются при использовании RAG. Если списываются - значит все работает правильно! ✅
