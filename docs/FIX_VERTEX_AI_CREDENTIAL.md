# 🔧 Решение проблемы "secretOrPrivateKey must be an asymmetric key"

## Проблема

Ошибка `secretOrPrivateKey must be an asymmetric key when using RS256` возникает, когда приватный ключ вставлен **без правильных переносов строк**.

В JSON файле ключ хранится как строка с `\n` (escape sequences), но в n8n нужно вставлять ключ с **реальными переносами строк**.

## ✅ Решение

### Вариант 1: Использовать скрипт (рекомендуется)

```bash
./scripts/get-private-key-for-n8n.sh
```

Скрипт выведет ключ с правильными переносами строк - просто скопируй весь блок и вставь в n8n.

### Вариант 2: Вручную

1. **Открой credential "Vertex AI Pro Account" в n8n**

2. **Полностью очисти поле "Private Key"** (Ctrl+A, Delete)

3. **Скопируй ключ из скрипта:**
   ```bash
   ./scripts/get-private-key-for-n8n.sh
   ```

4. **Вставь в поле Private Key** - важно, чтобы каждая строка была на новой строке:
   ```
   -----BEGIN PRIVATE KEY-----
   MIIEvAIBADANBgkqhkiG9w0BAQEFAASCBKYwggSiAgEAAoIBAQDpYeKVdAHODumy
   NTrrwOd064a7hEvK5GHXFdFCjl1DuLKHK4/HooFzedTl2o4qX2NM3/Y3cK2cjAPp
   ...
   -----END PRIVATE KEY-----
   ```

5. **Проверь формат:**
   - ✅ Должно быть `-----BEGIN PRIVATE KEY-----` в начале
   - ✅ Должно быть `-----END PRIVATE KEY-----` в конце
   - ✅ Каждая строка должна быть на новой строке (не всё в одну строку!)
   - ❌ НЕ должно быть `\n` как текста

6. **Сохрани** (Save)

7. **Проверь соединение** (Retry) - должно показать "Connection tested successfully"

## 🔍 Как проверить, что ключ правильный

После вставки ключа в n8n:
- Нажми "Retry" рядом с зелёным баннером
- Если видишь "Connection tested successfully" - ключ правильный ✅
- Если видишь ошибку - проверь формат ключа (переносы строк)

## 📚 Источники

- [n8n Community: RS256 Error Fix](https://community.n8n.io/t/error-secretorprivatekey-must-be-an-asymmetric-key-when-using-rs256-with-google-service-account-credential-in-n8n/109959)
- [n8n Docs: Google Service Account](https://docs.n8n.io/integrations/builtin/credentials/google/service-account/)

## ✅ Что уже исправлено

- ✅ Workflow URL: `gemini-1.5-flash-001:generateContent`
- ✅ JSON Body: `inlineData` (camelCase)
- ✅ MIME type: `audio/ogg`
- ✅ Workflow активирован

**Осталось только исправить формат приватного ключа в credential!**
