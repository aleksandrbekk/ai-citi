# 🔄 ИНСТРУКЦИЯ: Синхронизация работы в команде

## Проблема
Работаем в двоем с **одним аккаунтом Cursor** на разных устройствах. Нужна синхронизация.

## Решение
Используем **Git** + файл **`/docs/TEAM_STATUS.md`** для синхронизации.

---

## 📋 ЕЖЕДНЕВНЫЙ WORKFLOW

### 1. Утром / Перед началом работы

```bash
# 1. Получить последние изменения из GitHub
git pull

# 2. Прочитать статус команды
cat docs/TEAM_STATUS.md
# или открой файл в редакторе

# 3. Проверить, над чем работают другие
# Смотри раздел "ТЕКУЩАЯ ЗАДАЧА" и "АКТИВНЫЕ ЗАДАЧИ"
```

### 2. Перед началом новой задачи

1. Открой `/docs/TEAM_STATUS.md`
2. Найди раздел "АКТИВНЫЕ ЗАДАЧИ"
3. Добавь свою задачу или обнови статус существующей
4. Укажи, что ты начинаешь работу

**Пример:**
```markdown
| Задача | Агент | Статус | Дедлайн |
|--------|-------|--------|---------|
| Edge Function для автопубликации | БОРЯ | 🟡 В работе | - |
```

### 3. Во время работы

- Работай только со своими файлами (см. зоны ответственности)
- Если нужно изменить файл другого агента — сначала обсуди
- Регулярно делай коммиты (не оставляй на конец дня)

```bash
# Частые коммиты — хорошо!
git add .
git commit -m "feat: добавил функцию X"
git push
```

### 4. После завершения задачи

1. Сделай финальный коммит:
```bash
git add .
git commit -m "feat: завершил задачу X"
git push
```

2. Обнови `/docs/TEAM_STATUS.md`:
   - Отметь задачу как выполненную ✅
   - Убери из "В работе", если завершена
   - Добавь комментарий о том, что сделано

---

## ⚠️ ЧТО ДЕЛАТЬ ПРИ КОНФЛИКТАХ

### Если `git pull` показывает конфликт:

```bash
# 1. НЕ ПАНИКУЙ!
# 2. Посмотри, какие файлы конфликтуют
git status

# 3. Если конфликтуют ТВОИ файлы:
#    - Открой файл, найди маркеры конфликта (<<<<<<, ======, >>>>>>)
#    - Реши конфликт вручную
#    - Сохрани файл

# 4. Если конфликтуют ЧУЖИЕ файлы:
#    - НЕ РЕШАЙ САМ!
#    - Свяжись с другим разработчиком
#    - Решайте вместе

# 5. После решения конфликта:
git add .
git commit -m "fix: разрешил конфликт в файле X"
git push
```

### Если другой разработчик работает над тем же файлом:

1. **Свяжись с ним** (Telegram, сообщение в `TEAM_STATUS.md`)
2. **Решите, кто делает первым**
3. **Тот, кто делает вторым** — делает `git pull` после первого коммита

---

## 🎯 ПРАВИЛА РАБОТЫ

### ✅ ДЕЛАЙ:
- ✅ Всегда делай `git pull` перед началом работы
- ✅ Обновляй `TEAM_STATUS.md` когда начинаешь/заканчиваешь задачу
- ✅ Делай частые коммиты (не реже раза в час)
- ✅ Пиши понятные сообщения коммитов: `feat:`, `fix:`, `refactor:`
- ✅ Работай только со своими файлами

### ❌ НЕ ДЕЛАЙ:
- ❌ НЕ коммить без `git pull` (можешь потерять чужие изменения)
- ❌ НЕ трогай файлы других агентов без обсуждения
- ❌ НЕ оставляй конфликты нерешенными
- ❌ НЕ работай над одной задачей одновременно (обсудите, кто делает)

---

## 📝 ФОРМАТ КОММИТОВ

Используй префиксы для понятности:

```bash
feat: добавил новую функцию
fix: исправил баг
refactor: переписал код
docs: обновил документацию
style: изменил стили
test: добавил тесты
```

**Примеры:**
```bash
git commit -m "feat: добавил Edge Function для автопубликации"
git commit -m "fix: исправил ошибку загрузки медиа"
git commit -m "docs: обновил TEAM_STATUS.md"
```

---

## 🔍 ПРОВЕРКА ПЕРЕД КОММИТОМ

Перед `git push` проверь:

1. ✅ Код компилируется: `npm run build`
2. ✅ Нет TypeScript ошибок
3. ✅ Обновил `TEAM_STATUS.md` (если нужно)
4. ✅ Написал понятное сообщение коммита

---

## 💡 СОВЕТЫ

### Для быстрой синхронизации:
1. **Используй GitHub Desktop** или другой GUI клиент — проще видеть изменения
2. **Настрой уведомления** в GitHub на новые коммиты
3. **Создай алиасы** для частых команд:
   ```bash
   # Добавь в ~/.zshrc или ~/.bashrc
   alias gs="git status"
   alias gp="git pull"
   alias gpu="git push"
   alias gaa="git add ."
   alias gcm="git commit -m"
   ```

### Для избежания конфликтов:
1. **Работайте над разными задачами** одновременно
2. **Обсуждайте большие изменения** заранее
3. **Используйте ветки** для экспериментальных фич (опционально)

---

## 🆘 ЕСЛИ ЧТО-ТО ПОШЛО НЕ ТАК

### Потерял изменения?
```bash
# Посмотреть последние коммиты
git log

# Вернуться к последнему коммиту (ОСТОРОЖНО!)
git reset --hard HEAD

# Или вернуться к конкретному коммиту
git reset --hard <commit-hash>
```

### Случайно закоммитил не то?
```bash
# Отменить последний коммит (но оставить изменения)
git reset --soft HEAD~1

# Или полностью удалить последний коммит
git reset --hard HEAD~1
```

### Нужна помощь?
- Проверь `/docs/TEAM_STATUS.md` — там могут быть заметки
- Свяжись с другим разработчиком
- Посмотри историю коммитов: `git log --oneline`

---

## 📞 КОНТАКТЫ

Если нужна помощь с синхронизацией:
- Оставь комментарий в `/docs/TEAM_STATUS.md`
- Или свяжись напрямую с другим разработчиком

---

**Помни:** Git — это наш друг! Используй его правильно, и всё будет хорошо. 🚀

