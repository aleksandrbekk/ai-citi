# Расширение темы через AI Generate Content

## 🎯 Как работает сейчас

### Полный путь:

1. **Пользователь** → выбирает формат, стиль, аудиторию, вводит тему
2. **Frontend** → отправляет в N8N:
   - `topic`: тема от пользователя
   - `templateId`: формат карусели (mistakes, myths, steps и т.д.)
   - `style`: стиль дизайна (ai-citi, minimal, bright)
   - `audience`: целевая аудитория (networkers, experts, moms, freelancers)
   - `customAudience`: своя ЦА (если audience = custom)
3. **AI Generate Content** (gemini-2.0-flash) → расширяет тему:
   - Использует промпт из **Agent vasya v5.1.md**
   - Расширяет тему с учетом формата, стиля, аудитории
   - Возвращает JSON с переменными для шаблона
4. **Merge AI Result** → объединяет исходные данные + расширенные переменные
5. **Parse Request** → создает слайды с промптами для Imagen 4
6. **Gemini Generate** (Imagen 4) → генерирует изображения

## 📋 Что передается в AI Generate Content

```json
{
  "topic": "5 ошибок",
  "templateId": "mistakes",
  "style": "ai-citi",
  "audience": "networkers",
  "customAudience": ""
}
```

## 🔄 AI Generate Content расширяет тему

Используя промпт из **Agent vasya v5.1.md**, Gemini расширяет тему и возвращает:

```json
{
  "error_1": "Спамишь холодным трафиком",
  "error_2": "Не ведешь воронку",
  "error_3": "Нет системы в работе",
  "error_4": "Игнорируешь автоматизацию",
  "error_5": "Не анализируешь метрики",
  "viral_target": "ТОМУ КТО СЛИВАЕТ КОМАНДУ"
}
```

## ✅ Результат

Теперь тема пользователя расширяется качественным промптом с учетом:
- Формата карусели
- Стиля дизайна
- Целевой аудитории
- Контекста проблемы

Это обеспечивает высокое качество контента, как в рабочей модели с Клодом!

